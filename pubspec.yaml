name: app_manager
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  android_intent_plus: ^1.0.2
  # Flutter 的一些动画
  flutter_spinkit: "^4.1.2"
  shortcut: ^0.0.1
  path: ^1.8.0
  get: ^4.1.3
  global_repository:
    git: https://github.com/nightmare-space/global_repository
  flutter_svg: ^0.22.0
  hive: ^2.0.4
  apputils:
    git: https://github.com/nightmare-space/apputils
  adbutil:
    git: https://github.com/nightmare-space/adbutil
  pseudo_terminal_utils:
    git: https://github.com/Termare/pseudo_terminal_utils
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    
dependency_overrides:
  global_repository:
    path: /Users/nightmare/Desktop/nightmare-space/global_repository
  apputils:
    path: /Users/nightmare/Desktop/nightmare-space/apputils